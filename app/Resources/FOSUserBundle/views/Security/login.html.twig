
{% extends 'base.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}
{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
<h1 class="h1_title"> Page de connexion </h1>
<div class="row">
<div class="login col-md-6" >
<form action="{{ path("fos_user_security_check") }}" method="post">
    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

    <label for="username">{{ 'Votre username : '|trans }}</label>
    <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" />

    <label for="password">{{ 'Votre password : '|trans }}</label>
    <input type="password" id="password" name="_password" required="required" />

    <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
    <label for="remember_me">{{ 'Se rappeler de moi'|trans }}</label>

    <p class="logged_in"><input type="submit" id="_submit" name="_submit" value="{{ 'Se connecter'|trans }}" class="btn-primary button_login"/></p>
</form>
</div>
</div>
<div class="row">
    <div class="submit_login col-md-6">
        {{ 'Vous n\'êtes par encore inscrit ? '}} <a href="{{ path('fos_user_registration_register') }}"> S'enregistrer </a>
    </div>
</div>

{% endblock body %}
